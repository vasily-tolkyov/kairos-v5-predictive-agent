# 准备动作前消费被动经验的本机结果

本记录对应 R5 源码 `432be4c7bda4fd1da616261ddcd6b3348d45750d`，是持续推进中的一次有序开发对照。长期自主学习、有效保持和开放多阶段任务仍待实机证明。

R5 在规划结束、请求准备帧之前，只消费一次积压的真实被动窗口并验证、学习。准备回调仍先学习此后到达的新被动经验，再做新预测和完整帧校验；过期仍拒绝。所有阶段共用重复、冲突及时间顺序检查，冻结学习时也不绕过检查。共享边界除序号相同外，完整帧摘要也必须相同；已接受的部分消费结果在取消、故障时保留。每次决策至多准备一次、至多等待一次下一帧，定时器请求仍为 50ms；目标、支持阈值、动作时长、搜索和决策预算保持原登记值。

旧源码在新增的“规划后被动学习”反例上失败。第一次候选专项 55/55 通过后，新增共享边界冲突反例仍使它失败；其中一个夹具还错误地后退了当前序号，修正夹具后反例继续失败。最终专项（含新增反例）60/60 通过，耗时 1.869 秒；完整集成 229/229 通过，耗时 66.176 秒。早期测试类型错误、夹具错误及所有失败输出均保留在 [verification](verification/manifest.json)。`commands.json` 是完整集成之前的专项记录，其中 `fullGateRuns: 0` 不表示后续未运行完整集成；后者以 `full-gate.log` 为据。空编译日志仅表示没有编译诊断，退出码由命令记录说明。

本轮与 R4 `949cd496c0adb5a36cbb878f72e63029a3bbb874` 使用同一停止世界、初始 session 字节、恢复模型、选择计数、目标以及 32 决策 / 180 秒上限。初始学习摘要为 `3566f66c62c83bdfcf6a54e7c8c9e4a4bb5a2e178e621a91e2d0ef6560edd8f5`。结果取自已停止原始目录 `evidence/pr3-local-predrain`，并由独立对齐和覆盖审计交叉核对。

| 本轮新增或实际测量 | R4 对照 | R5 候选 |
| --- | ---: | ---: |
| 全部决策 | 32 | 32 |
| 实际执行窗口（含主动观察） | 8 | 22 |
| 过期拒绝 | 24 | 10 |
| 新学习写入 | 97 | 125 |
| 其中被动窗口 / 被动写入 | 89 | 103 |
| 受支持的执行预测 / 任务计划 | 0 / 0 | 0 / 0 |
| 终点目标 | pending | pending |
| 报告实际秒数 | 55.148 | 48.502 |

R5 的 22 个执行窗口包含一次主动观察，不能全称为持续按键动作；10 个窗口带有实测电机收据，零个为零时长电机暴露。全部 10 次拒绝均保留在分母。终点轴没有受支持的预测区间，不能把覆盖率写成预测准确率。学习累计写入从 24 到 149，被动写入从 23 到 126，决策累计从 8 到 40，执行累计从 1 到 23；累计量与原始窗口、决策日志一致。两轮均记录零死亡。

对齐审计核对 R5 序号 34 到 563 的 529 个连续已归档相邻真实观测间隔，没有重复、缺口或共享帧冲突，接受执行的首帧与准备帧一致。另有 58 个检查点 / 关闭阶段的未归档尾间隔，单列于审计，不计入这 529 个已学习区间。这里的相邻观测间隔不替代每帧独立物理时钟，也不据此为旧记录补造按键持续时间。审计的 R4 负例按预期拒绝缺少新消费阶段计数的旧输出，错误为 `missing pre-preparation drain accounting`，不是 R5 实机失败。

运行于 `2026-09-13T10:26:21.198Z` 按预算停止，状态 `budget-paused`。停止审计的计数、日志及保存玩家位置一致，日志冲突为空。最终学习摘要为 `17e143c299aa6ce51d7f6f0db36708b5803209402b85f89119b9550348a1d829`，最终 session SHA-256 为 `cdadeeba1476a0a3d250c308b3d5ad98a48b83671561593e474ad0e1d82f5000`，停止世界归档摘要为 `b1a8593a7fbac66b64bed4a9a6f8a8625c583aa50f4c687d0f9c6cf5910659f8`。离线恢复核对保持学习和任务状态、清除 381 个临时可见绑定，只读预测没有写入，重复原窗口被拒绝；这是停止检查点的恢复验证，不是新增实机重启成功证据。

预登记有一次发车前修正：`controlProtocolSha256` 最初误用了完整 `protocol.json` 文件字节摘要 `d88d3c0708dc8c7f4da439d7e8ef4ef80e863e9c976ea934bda9ba6ab73522b3`，随后按匹配器约定改为 `SHA256(JSON.stringify(controlReport.protocol))`，即 `c6b5bc2c7446fd36b1f137fa79856d9c0a700c8af3a39008b2dd526bb33b3ad8`。修正没有改变目标或预算；最终 [preregistered.json](preregistered.json) 摘要为 `5859318a5fb889db091de663b49c234fbd5226820511127a70b7ea2749b7b3b5`，也是对齐审计记录的登记摘要。这里保留修正事实，不将修正前登记的完整字节声称为已归档材料。

这次对照只支持执行可用性由 8/32 到 22/32 的有限改善。新增消费会冲刷部分被动窗口，改变窗口划分、时长分布和后续模型轨迹，因此不是保持模型逐字节等价的耗时优化；48.502 秒也不能外推为总体因果提速。可信预测和目标仍未出现，尚不能成立有益的 A→B→A 保持或陌生前提组合能力。另行登记的原始和平世界 256 决策经验获取正在继续；后续固定任务对照预算仍是原始 **192 决策 / 480 秒**，历史 191 次实际执行不等于预算。

冻结 actor 目录为 `evidence/execution-builds/pr3-local-r5-predrain`。本目录的 `verification` 只原字节复制 `evidence/pr3-preparation-drain-recovery` 中已停止的本轮测试、运行和审计小文件；[manifest.json](verification/manifest.json) 记录来源、字节数和 SHA-256。正在写入的 `reacquisition-run.log` 不属于本轮停止归档，不包含在副本中。整理文档没有重跑测试、模型查询或实机动作，没有改动 actor、原始事件或停止世界。
