# 同一窗口的重复统计重算

R7 在线 1/22、冻结 5/32 次新执行，目标和受支持预测均为零。改动在原始窗口的全部事前误差计算完成后，合并对外不可见的重复叶统计重算。周期分裂仍在原来的每 16 行位置发生，淘汰仍使用相同顺序和修剪后的分区；最后完整刷新统计，外部读取前不存在陈旧校准。

固定旧构建和候选复放 R6 的全部 150 个被动窗口，都逐字得到实际终点模型 SHA `72eef0525b987ddd02f050e3ef698690b1cc069d7eae4a78fed7ecd0e6169965`。采样学习耗时分别为 23,589.621 与 16,675.697 毫秒，状态同化 2,737.548 与 2,640.228 毫秒。另有容量 32/48/192 的合成差分，192 个原始窗口、3,072 次查询的保留样本、分区结构、误差、区间和支持逐项相同。这些属于离线软件性能和等价性证据，不能算新增经验或实机速度倍率。

原始 CPU 采样及完整日志在 `evidence/pr3-continuous-profile`，差分和软件检查在 `evidence/pr3-window-statistics-recovery`。完整集成门禁、源码提交和新的 639+ 文件冻结身份完成后，才启动 [事前登记](preregistered.json) 的在线/冻结两组：新的原始 smoke 世界和相同模型副本，各 32 决策 / 180 秒，Peaceful。实际结果、全部拒绝、源窗口、实际时间和目标继续独立记录，不放宽支持、动作或搜索条件。
