# 异步开始帧：代码与实机复核

当前代码提交 `85175eeb00227ff5dd6e46676bbc8e1c543c0ab8`，源码树 `a2bbb21c8934eaab7f4b55df2316128fb7b318dc`。完整标准门禁 **184/184**，0 失败、取消或跳过；[集成日志](integrated-gate.log)、[协议及红绿回归](PROTOCOL.zh-CN.md)、[运行前登记](protocol.json)、[864 个执行副本文件的身份](actor-manifest.json) 均保留。

| 实测项目 | 结果 | 能证明什么 |
|---|---|---|
| 启动尝试 | 2 次：1 次在模块加载阶段失败，1 次进入游戏 | 失败尝试没有从启动分母删除；第一次没有 Java、决策或动作 |
| 和平 worker | 8 决策，4 执行、4 `action-start-expired` 拒绝；约 17.339 秒 | 原 8 决策/120 秒预算，没有内部重试或追加预算 |
| 完整开始帧 | 4/4 已执行动作与准备 token、实际首帧摘要一致 | 执行比例 4/8=50%；已执行动作的帧一致率 4/4=100%，两者分母不同 |
| 预测支持与任务 | 受支持结果字段 0，可比较字段 0，受支持任务计划 0；目标 pending | 没有预测准确率或区间覆盖率分母；不能把默认 `basis: supported` 且空字段当成实质支持 |
| 真实经验 | 4 主动窗口、17 被动窗口、21 次写入、0 死亡 | 冷启动短跑；不能证明任务收益、保持、迁移或生存能力 |
| 连续记录 | 初始 31→32 缺 1 个间隔；其后动作间等待的 27 个间隔有被动窗口覆盖 | 缺口原样报告，没有补帧；不能称整个启动过程无缺口 |
| 停止世界与恢复 | 保存玩家与最终观察一致；模型/任务保持，暂态可见绑定清除；查询只读、重复窗口不写入 | 离线检查点恢复，不是跨重启原生任务验收 |
| 动作时长校准 | 6 次请求 4 ticks 均为实际 4 ticks，含 230 ms CPU 阻塞 | 独立控制计数、实际动作收据、服务端位置相符；装置经验未进入自主模型 |

准备到核验约 12.98–63.81 ms，包括被动窗口处理、预测和线程通信；不能据此声称单次预测的最坏时间已测定。RSS 样本约 441–584 MB，短跑不能证明长期资源有界。

第一次启动失败源于先前跨临时目录的依赖链接失效。离线安装因缓存不完整失败后，从官方 npm 仓库重新安装原锁定依赖，192 个包版本逐项匹配、原锁文件逐字节未变、完整性校验保留；当前依赖目录已独立实体化。锁中没有安装脚本，因此安装使用 `--ignore-scripts`。已有编译文件和全部执行副本内容均未改变。启动和安装失败的原始日志均保存，不能将环境修复后的成功倒写成第一次启动成功。

[原生汇总](native/summary.json)、[原始文件清单](native/MANIFEST.json)、[归档二次复读校验](artifact-double-check.json)、[帧摘要审计](native/audits/worker-fresh-v1-freshness-audit.json)、[停止世界审计](native/audits/worker-fresh-v1-stopped-audit.json)、[恢复审计](native/audits/worker-fresh-v1-restore-audit.json)、[时长校准审计](native/audits/worker-fresh-motor-v1-audit.json)、[依赖恢复记录](native/dependency-recovery/) 保存对应证据。原首批 actor 的结果仍属于首批提交，不能归到当前 actor。

工程包 A 的严格原始模型/世界对照和预测支持/可靠性验收仍未通过，B–E 未进入。需要取得原计划指定的三份原始实验 ZIP，并校验 5098/12321-write 模型、frame 19 和世界身份；这次新世界不能替代它们。
