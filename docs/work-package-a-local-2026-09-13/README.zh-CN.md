# PR #3 本机续接

读取并核对远端 main `fb3c40a8e172e092ca6ba9a69eebc2836e255b36` 与 PR #3 `58f06711fd0a3b882dcc39899b75b9e88f0e9fa6`。两份用户附件与 PR 中输入文档共同定义工程和验收方向；文档中的历史执行记录不是新结果，旧自定义运行协议不再执行。

云端中断候选源码没有上传。本机从 PR 已发布源码重新实现：

- 初始采集与边界帧在 worker 内同步取得，控制器采用回包中的原帧；较新缓存不能替换初始边界。
- 每个真实窗口内所有对象使用窗口开始前的网络与上下文预测。新输出类别在评分后进入分类词表；所有行仍参与拟合，校准按来源窗口计数并分别取最大标准化误差、最大实际残差。
- ContextualReadout3 保留旧参数、样本及原始误差；旧行没有窗口身份，不伪造独立校准。新真实窗口可重新建立支持。运动后备也按有界的独立窗口校准，并服从局部支持域、实际残差和反驳；法向符号使用等价的有向约束转换。
- 搜索保留字段存在性、支持、几何与目标身份，仅规范化未知属性的占位值。测试核对后续预测和可用动作等价；十阶段测试原断言、128/512 节点及原生墙钟预算未修改。

本机 Node 24.14.0，TypeScript 5.9.3，原锁依赖由 npm ci 恢复。PR 基线完整门禁184/184。新窗口反例初跑5项中4失败、1通过；窗口首候选专项33/33。完整候选1为192项中191通过、1失败，复现十阶段session搜索预算耗尽。搜索修复专项60/60。增加运动局部支持域的首候选暴露法向符号问题，保留失败；修正后完整门禁196/196，约89.661秒。测试均为软件证据，未替代真实游戏验收。

原始三个ZIP已从本机恢复，重新计算SHA-256全部匹配：原生包20716418…ff90e，运动包ea9c486e…64ba，R6保持包c101005a…639。旧5098/12321-write检查点、原frame19及原始世界可读。不会用V50/V51或新世界代替这组严格对照。

## 新增本机实测

| 独立运行 | actor源码 | 新决策 / 执行 / 拒绝 | 新学习写入 | 结果 |
|---|---|---|---|---|
| 初始和平短跑 | fe5d912 | 8 / 1 / 7 | 24 | pending；0受支持执行预测 |
| 原检查点计时对照 | 99cadb7 | 32 / 3 / 29 | 97 | pending；0受支持执行预测 |
| 只复制读出参数的配对候选 | bf1854a | 32 / 6 / 26 | 90 | pending；0受支持执行预测 |

三次均实际启动Minecraft 1.21.4，和平规则经服务端确认，零死亡。合计72决策、10实际自主动作/主动观察窗口、62拒绝、211新写入，其中201是被动窗口。不能将分支共享的24-write初始模型重复算作新经验，也不能把和平零死亡当作敌对生存能力。

另有6次隔离装置选定的运动校准，三个无负载/注入230ms阻塞配对均精确按4个物理tick释放，客户端实际位置与服务端读数匹配；零自主选择、零学习写入。这些校准数据没有进入自主模型。

采集审计分别核对58/271/363个真实间隔，初始至最后归档帧无缺口、重复或共享帧冲突。后续检查点/关闭尾部另有8/34/37个未归档间隔，明确不计作经验。三次停止世界审计与动作开始token审计通过；首轮及最终候选快照恢复、只读查询、重复事件拒绝通过。后两次是真实跨进程恢复到已复制世界的运行，不能只靠离线恢复断言替代。

## 已定位并实际改善的延迟

原32决策中，回调内校验/被动学习/预测的平均耗时为6.16/37.37/9.45ms。全部97个原始窗口按实际顺序重放，精确重建停止模型摘要 `d7658685ae7a510e45bf472d4eeb89cf88bf693a6b7287d531f2b15d6a14c825`；CPU采样发现主要开销为复制拟合协方差。

候选仅复制读出所需参数，保留完整的窗口前预测隔离。相同97窗口重放仍逐字节得到同一最终模型；单次离线重放累计学习耗时由2799.95ms到1195.80ms。最终actor完整门禁196/196，约64.217秒。

实机配对明确核对同一初始世界树、session原始字节、恢复后模型摘要、选择计数、目标和32决策/180秒预算。候选回调被动学习平均17.33ms，对照37.37ms；实际执行比例3/32到6/32。只有一组按对照→候选顺序进行的开发集试验，不能外推一般提速或任务收益；两者目标均未完成，受支持执行预测及受支持任务计划均为零。

原5098/12321-write模型的参数、原样本及误差、ledger和选择计数核对保留。旧模型分别有7324/9653条拟合行，均缺可核实的原窗口ID，因此没有被重新授予独立校准。相同原frame19下，在100/500/2000ms离线预算内两者受支持计划均为0，仍能提出明确不受支持的假设。这是校准权威迁移的限制，不是新实机保持失败的替代证据，更不是保持修复成功。

## 保留的失败和偏差

除了前述软件红灯，保留搜索测试构建类型错误及修正日志。第一次停止世界审计在归档完成前启动而失败；归档完成后重新审计通过，原失败保留。比较器首稿错误读取时长字段，生成的null统计被另存为 `matched-start-costs-invalid-duration-field.json`，不得作为有效汇总；修正后校验所有输入有限并重算。

首8决策短跑的启动时段与原模型只读画像进程有重叠，不作为无争用性能样本；后32/32配对期间没有同时运行CPU实验。没有自动重试实机启动，没有放宽帧相等条件、支持门槛、目标或原预算。

完整执行日志、原始窗口、停止世界、初始检查点和三份冻结actor保存在新增证据包中。精简可读审计在本目录 `verification/`；下载登记见 `artifact.json`。所有运行已停止。

整体A–E尚未验收。尤其旧校准被撤销不等于修复知识保持；需重新取得可信且有用的行动覆盖，完成同世界冻结/擦除/在线对照、A→B→A、终态限定的真实因果组合，然后再扩大时长和陌生世界。当前无长期自主学习或开放多阶段成功的新声明。

下一步应优先减少剩余26/32的开始帧过期，并在可信支持门槛不变时取得可用动作覆盖；再推进持续状态与保持、真实前提组合。不可直接把未通过简单和平任务的现状扩成8/24小时压力测试，也不可宣称B–E已实现。
