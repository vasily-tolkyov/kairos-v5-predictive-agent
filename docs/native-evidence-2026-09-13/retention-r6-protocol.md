# Exploratory R6 frozen retention check

Registered before preparation and execution. Recreate the original peaceful
initial world from its stopped archive, preserve every world byte and all
nonlearning initial state including choice counter 1801, then transplant only
dynamics and affordances from the stopped one-hour model. Preparation adds zero
physical trials. Use the exact existing held-out goal z < -14.5, introduce it
at decision zero, freeze learning, and stop at 192 decisions / 480 seconds or
verified success. Run sequentially on port 25632 with the immutable R6 actor.

The reference is the earlier stopped R3 post-learning test, which failed after
191 actions. R6 also contains the R5 recovery-progress change and later harness
guards. This is an exploratory composite candidate check, not an isolated
actuator-effect estimate, an additional learning episode, or evidence of
improved dynamics retention. Audit original windows, unchanged learned digest,
saved player/world and any actual release receipts. No evaluator action recipe,
subgoal order or hidden outcomes are passed to the learner.
