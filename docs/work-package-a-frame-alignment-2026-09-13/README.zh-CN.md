# 单次真实帧对齐的本机结果

这是持续推进中的一个装置实验，不是任务完成报告。用户已明确允许分派子任务；主任务负责整合、实机执行和继续验收。

执行源码 `949cd496c0adb5a36cbb878f72e63029a3bbb874`。准备动作时只等待一次下一真实帧，定时器请求50ms；超时使用当时实际帧，完整序号与摘要比较不变。取消、替换、故障、关闭和死亡停钟均有专项验证。原源码两个新增反例失败，候选专项63/63、完整集成207/207通过（76.029秒）。早期类型/夹具错误及被中断的夹具测试均保留，未改生产超时或原断言。冻结副本逐文件核验617个文件；其中包含单独新增、尚未提交的覆盖审计脚本，该脚本不参与actor决策。

在与 `bf1854a` 相同的停止世界、原session字节、恢复模型、选择计数、目标及32决策/180秒限制下，新候选执行8次、过期拒绝24次，对照执行6次、拒绝26次。新增97个学习窗口，其中89为被动窗口、8为自主选定动作/主动观察。所有32次等待均由下一真实帧结束。仍为零可信执行预测、零受支持任务计划、目标pending、零死亡；发生一次动作窗口内的健康下降，和平规则不排除跌落伤害。

独立审计核对523个连续真实间隔，无重复、缺口或共享帧冲突；接受动作的原始首帧与准备帧一致。停止后世界/玩家位置核对通过。快照恢复保持学习和任务状态，清除131个临时可见绑定；只读查询不写入，重复原事件被拒绝。末检查点SHA-256为 `b79be80aa491aade7215144c43bc858c280abf8a3d19b261851ee1dda113bfce`，停止世界归档摘要 `343f2a368a79ba403bc9abd5aaa67e6a04569fa7495278777af65048ee434f5f`。

一次有序开发对照只支持执行可用性从6/32到8/32的有限改善。回调被动学习平均耗时从17.33ms变为33.66ms；等待改变实时轨迹，不能把本次结果说成总体提速，更不能认为学习保持已修复。24/32过期仍是主要限制。

后续继续单独验证在准备token前消费积压被动经验，严格保留预测前学习及帧校验。该新增drain会冲刷部分被动窗口，可能改变窗口划分、时长电路和模型轨迹，已在另一份预登记中明确，不能称为逐字节模型等价的耗时优化。原任务的256决策获取试验及192决策/480秒三组对照也已另行登记；不会把历史191次实际执行误改为预算。

原始目录为 `evidence/pr3-local-frame-alignment`，冻结actor为 `evidence/execution-builds/pr3-local-r4-frame-alignment`，检查输出和失败日志在 `evidence/pr3-frame-alignment-recovery`。这一轮没有取得长期自主学习或开放多阶段成功证据，后续工程和实机验收继续进行。
