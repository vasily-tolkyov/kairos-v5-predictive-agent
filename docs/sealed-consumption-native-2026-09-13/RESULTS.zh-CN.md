# R9 积压故障与未启动对照

R9 在线试验失败。9 次完成决策中执行 4 次、开始过期拒绝 5 次，未完成目标，也没有受支持的执行预测。第 9 次决策耗时 80,444 毫秒，其中延后学习 45,500 毫秒。随后被动采集消费者落后并触发故障，实际停止报告为 320.136 秒，超过原始 180 秒软边界；未增加预算或补齐决策。

服务器于 `2026-09-13T12:42:00.215Z` 完成保存和停止。保留了 200 个完整原始窗口，4 主动、196 被动，200 次新学习写入；没有死亡。归档中的 1,875 个间隔连续，但遥测出现丢失，部分已同化归档尾部没有完成决策的水位记录，而且故障连接未提供新的末帧。**实机完整性未通过，不能使用 4/9 的执行率声称改善。** 根据 [事前登记](preregistered.json)，[冻结对照没有启动](skipped-frozen.json)，原始起点副本保持未使用。

旧停止核验器在末帧缺失时抛错，旧窗口核验器把未知关闭尾部显示为 0；这些原始日志和输出全部保留。修正后的独立核验结果同时保留：末帧、关闭尾部和最终玩家位置一致性均为未知，完整性为 incomplete。初始化还缺少归档之前的已同化帧，不能独立重建完整实时初态。

故障时 session SHA-256 为 `48273ef1e54c2f16de37e93b4d5481e23c04e381a19ca70bb4ec852a1e9945ae`，停止世界为 `7640ad4c0926c236aca6a1eb5214a803d646598dd771c732964781a1ef27e726`。冻结执行源码为 `f21ed79728a6c17ad99a970b2e672d72ea15eb9c`，651 个文件。故障检查点离线恢复校验通过，参数/任务不变，清除实时时钟和激活，仅保留未重新锚定的回忆；这不是实机重启能力证据。完整核验副本见 [清单](native-verification/manifest.json)。

后续候选针对有变化的场景中分区搜索成本，并补齐原始初始化帧与故障退出。原始目标、支持阈值、实际动作和搜索预算保持原值；长期自主学习、保持和开放多阶段验收继续进行。
