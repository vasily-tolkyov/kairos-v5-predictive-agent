# 连续状态首片：原生结果

两组均按事前登记完成 32 次新决策并停止。**真实状态与参数冻结的分离取得了短样本接入证据；两组均未完成目标，没有受支持的执行预测，没有长期学习或多阶段能力结论。**

| 条件 | 新执行 / 新决策 | 过期拒绝 / offer 不可用 | 新参数写入 | 完整窗口 | 归档真实帧 | 原始目标 |
| --- | ---: | --- | ---: | ---: | ---: | --- |
| online | 0 / 32 | 32 / 0 | 150，全部被动 | 150 | 907 | 待完成 |
| frozen | 14 / 32 | 17 / 1 | 0 | 126（14 主动，112 被动） | 429 | 待完成 |

源 actor 为 `6890883138b88008332e6b86c1ae8dc6ad71a5b2`，639 个冻结执行文件，源世界/模型均由事前登记的 smoke 停止状态新建副本。目标 `position.2 < -5.5` 只属于这组接入试验，不替换原始和平任务的 `-14.5` 目标。完整启动身份见 [launch-pins.json](launch-pins.json)。在线实际耗时 66.476 秒、冻结 41.705 秒，均未追加预算。

两组原始窗口连续性、完整共享边界、物理时钟、预测开始帧绑定、消费水位、动作与服务器停止状态核验均通过。冻结组覆盖 7 个实际 held receipt、96 个 held 间隔及 35 个释放后 off 间隔；另有 6 个未测量冲量边沿，不把它们假称已知运动。两组均未发生死亡，零时间终端采样只被软件反例覆盖。两组初始消费计数均已与归档帧数衔接，未出现额外锚点计数。检查点/关闭尾部的在线 44、冻结 21 个未归档间隔单列，不计作已消费经验。

冻结组完整动力学与可供性对象等于源模型，逐帧状态仍推进到实际 sequence 462；运行中消费计数、起点摘要和最后归档帧吻合。在线参数由 24 次历史写入增至 174 次，150 个新独立单位都是原始被动窗口，子间隔未获得独立窗口身份。模型改变不等于有用学习。

在线动作开始期间的被动处理和更新约占 100–400 毫秒，远高于同次未知动作预测的约 0.03 毫秒；全部动作因此过期。冻结组仍有 18 次拒绝，说明实时状态与其它准备开销也存在。不同轨迹和样本规模不能证明纯粹速度倍率。后续候选将保持真实帧及时同化，把准备阶段的参数拟合移到授权结果后，并维持完整被动窗口在动作结果之前的学习顺序；必须另立源码、冻结构建和登记。

原始输出分别位于 `evidence/pr3-local-continuous` 与 `evidence/pr3-local-continuous-frozen`，原始运行日志在 `evidence/pr3-continuous-state-recovery`。在线世界归档 SHA-256 为 `a1a44118f885ea2ae456d969a530cc410c7b9d051b487d52206329da5f357bcf`，冻结世界为 `ff18715be41eb544a36589045b4e662b71f3aea0397e3ecb9202fc54778ade92`。本结果不验收连续预演、历史的因果收益、A→B→A 保持、陌生前提组合或 1/8/24 小时任务。
