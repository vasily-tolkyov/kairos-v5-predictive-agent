# 把逐帧状态和授权时参数拟合分开

R6 在线组 32 次尝试全部过期；冻结组执行 14/32。真实时序接入核验均通过，目标和受支持预测均为零，详情见 [R6 实机结果](../continuous-state-native-2026-09-13/RESULTS.zh-CN.md)。

本候选在有真实逐帧状态的路径中，先同化每个实际帧并以精确开始帧的 z 预测；准备回调中的完整被动窗口保留原状态引用，在执行、拒绝或异常返回之后按原顺序更新参数，随后才学习动作窗口。没有删除窗口或放宽开始帧保护。开始预测会使用尚未学习这批准备窗口的 θ，这是明确的学习时序改变，不能当作相同模型的纯速度对比。每次记录实际 θ 版本、延后原窗口 ID、写入差值和拟合耗时。

专项 79/79；完整现行集成 **316/316**，66.149 秒，零失败、取消或跳过。新增四条执行路径反例及独立窗口/参数差值审计。首次编译的两个测试类型错误及全部输出保留在 `verification`；没有失败的实机尝试被重命名成成功试验。软件通过不等于改善实机结果。

[事前登记](preregistered.json) 固定原始 smoke 世界/模型、32 次决策 / 180 秒，在线然后冻结，各一次，目标保持 `position.2 < -5.5`，动作/搜索/支持条件不变。启动前须固定源码、构建、软件通过输出和两个相同的起点，单独写入 `launch-pins.json`。这不替换原始 `-14.5` 任务，也不验收长期自主学习或陌生前提组合。
