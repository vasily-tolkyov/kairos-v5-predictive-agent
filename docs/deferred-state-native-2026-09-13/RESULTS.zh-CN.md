# 延后拟合配对：停止结果

两组均已停止、保留世界和完整原始记录。**没有目标完成或受支持的执行预测；该改动没有在这次配对中展示可用的在线学习改善。**

| 条件 | 新执行 / 新决策 | 过期拒绝 | 新参数写入 | 归档间隔 / 未归档关闭尾部 | 实际耗时 |
| --- | ---: | ---: | ---: | --- | ---: |
| online | 1 / 22 | 21 | 378（377 被动，1 主动） | 3352 / 172 | 195.262 秒 |
| frozen | 5 / 32 | 27 | 0 | 393 / 23 | 45.107 秒 |

online 先达到原始 180 秒软时间边界，结束和检查点产生实际超时，未补满 32 次决策。frozen 保持原始模型，未使用在线终点。原始目标始终为这组 smoke 接入试验的 `position.2 < -5.5`，没有改写原始和平任务的 `-14.5` 谓词。

执行源码为 `2b37116b2cfd25181746f8d24a92c7e48686d8a6`，639 个冻结文件。完整窗口连续性、实测时钟和预测开始帧、延后原窗口/参数写入差值、服务器停止状态核验均通过；冻结模型的动力学和可供性对象与起点完全相同。两组均没有死亡。独立读取停止 checkpoint 后，参数、任务和非瞬态记忆保持一致，清空当前帧/物理钟/激活，仅保留未重新锚定的可见线索回忆；这是离线恢复验证，不是新的实机重启结果。

原始目录为 `evidence/pr3-local-deferred` 和 `evidence/pr3-local-deferred-frozen`。在线 session SHA-256 `d2fefb785f92663e8671e438baec5695a23451ad7ecf157b11ffdc5ea9f8a060`，停止世界 `7887d2b5f552341e900a2eff3cb1458c7435f951cfb0d641d0717822c387a256`；冻结 session `dbf1495bc9227361d4a15478a94090342a3e2734882a55c6c82041bb65c4d681`，停止世界 `65f2450aad6c522e85f65292b09c7c5e68b8f25ea169e90005fb925b95ffe98a`。完整 stdout/stderr 在 `evidence/pr3-deferred-state-recovery`。

统计拟合产生的积压仍然占据大量时间；单独移动更新时机不足以解决。下一候选合并同一原始窗口内的叶统计重算，并保留每行误差、周期分裂、样本淘汰和最终支持语义；须先验证与固定旧版本的逐项等价，再单独固定身份运行。不会将上述失败扩展预算或从报告中删除，B–E 的能力验收仍未满足。
